# Arbitrary_Precision_Calculator-APC-
Developed an Arbitrary Precision Calculator in C that performs arithmetic operations on very large numbers beyond the limit of standard data types. The program uses linked lists to store and process large integers, enabling accurate addition, subtraction, multiplication, and division of large values.
